import { makeStyles } from "@mui/styles";
export const useAuthenticationFormStyles = makeStyles((theme) => ({
    actions: {
        width: "100%",
        display: "flex",
        justifyContent: "space-between",
        alignItems: "center",
    },
}));
